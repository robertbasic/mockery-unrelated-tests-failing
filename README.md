# Mockery unmet expectation causing unrelated tests to fail

```bash
git clone git@github.com:robertbasic/mockery-unrelated-tests-failing.git
cd mockery-unrelated-tests-failing
composer install
./vendor/bin/phpunit
```
